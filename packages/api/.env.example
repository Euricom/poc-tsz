# Set to true to disable JWT validation on protected endpoints (local testing only).
# A warning is logged at startup when this is enabled.
# Auth__Disabled=true

# Microsoft Entra app registration (single tenant, shared with web)
Auth__TenantId=
Auth__ClientId=


# SQLite connection string for the animals DB
App__DatabaseUrl=file:./tsz.db
